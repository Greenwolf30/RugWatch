/**
 * Public site config (safe to commit).
 * Never put HELIUS / GITHUB_TOKEN / provider keys here.
 */
window.RUGWATCH_CONFIG = {
  // Backend root with no trailing slash. Empty = same host as this page.
  apiBase: "",

  // Optional: site passcode if server has WEB_API_TOKEN set.
  // Users can also enter it in the Gear field (stored in sessionStorage only).
};
